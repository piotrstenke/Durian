namespace Durian.Info;

/// <summary>
/// Names of all existing Durian packages.
/// </summary>
public static class PackageNames
{
	/// <summary>
	/// Name of the <see cref="DurianPackage.Main"/> package.
	/// </summary>
	public const string Main = nameof(DurianPackage.Main);

	/// <summary>
	/// Name of the <see cref="DurianPackage.Core"/> package.
	/// </summary>
	public const string Core = nameof(DurianPackage.Core);

	/// <summary>
	/// Name of the <see cref="DurianPackage.CoreAnalyzer"/> package.
	/// </summary>
	public const string CoreAnalyzer = nameof(DurianPackage.CoreAnalyzer);

	/// <summary>
	/// Name of the <see cref="DurianPackage.AnalysisServices"/> package.
	/// </summary>
	public const string AnalysisServices = nameof(DurianPackage.AnalysisServices);

	/// <summary>
	/// Name of the <see cref="DurianPackage.TestServices"/> package.
	/// </summary>
	public const string TestServices = nameof(DurianPackage.TestServices);

	/// <summary>
	/// Name of the <see cref="DurianPackage.DefaultParam"/> package.
	/// </summary>
	public const string DefaultParam = nameof(DurianPackage.DefaultParam);

	/// <summary>
	/// Name of the <see cref="DurianPackage.FriendClass"/> package.
	/// </summary>
	public const string FriendClass = nameof(DurianPackage.FriendClass);

	/// <summary>
	/// Name of the <see cref="DurianPackage.InterfaceTargets"/> package.
	/// </summary>
	public const string InterfaceTargets = nameof(DurianPackage.InterfaceTargets);

	/// <summary>
	/// Name of the <see cref="DurianPackage.CopyFrom"/> package.
	/// </summary>
	public const string CopyFrom = nameof(DurianPackage.CopyFrom);
}
